{
  "name": "renilham-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute renilham-db --remote --file ../worker/schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250712.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.110.0"
  }
}
